// import { EventFilters } from "@/types/eventFilters";

// export const getEventFiltered = async (options?: EventFilters) => {
//   await new Promise((resolve) => setTimeout(resolve, 1000));

//   let filteredEvents = [];

//   if (options?.search) {
//     filteredEvents = filteredEvents.filter((event) => {
//       return event.name.toLowerCase().includes(options.search!.toLowerCase());
//     });
//   }

//   if (options?.category) {
//     filteredProducts = filteredProducts.filter((product) => {
//       return product.category === options.category;
//     });
//   }

//   if (options?.maxPrice) {
//     filteredProducts = filteredProducts.filter((product) => {
//       return product.price <= (options.maxPrice as number);
//     });
//   }

// return filteredEvents;
// };
